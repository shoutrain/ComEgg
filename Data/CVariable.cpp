#include "CVariable.h"
/*
set_variable CVariable::m_VariableSet;

void CVariable::Load()
{
	for (set_variable::const_iterator pos = m_VariableSet.begin();
		 pos != m_VariableSet.end(); pos++)
	{
		((CVariable *)(*pos))->Initialize();
	}
}

void CVariable::Register(const CVariable *pVar)
{
	if (!m_VariableSet.count(pVar))
		m_VariableSet.insert(pVar);
}

void CVariable::Unregister(const CVariable *pVar)
{
	if (m_VariableSet.count(pVar))
		m_VariableSet.erase(pVar);
}
*/
